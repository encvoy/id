# Widget

🎯 **Компактный виджет аутентификации на чистом JavaScript** с поддержкой OIDC/OAuth2 и PKCE для безопасной интеграции в любые веб-сайты.

## ✨ Особенности

- 🚀 **Сверхлегкий**: всего ~6KB gzipped (уменьшение в 20+ раз по сравнению с React версией)
- 🔒 **Shadow DOM**: полная изоляция стилей
- 🎨 **Кастомизация**: настраиваемый акцентный цвет
- 🌐 **Универсальность**: работает в любом фреймворке или без него
- 🛡️ **Безопасность**: OIDC/OAuth2 с PKCE
- 📱 **Отзывчивость**: адаптивный дизайн

## 🚀 Быстрая интеграция

### Шаг 1: Сборка виджета

```bash
npm install
npm run build
```

**Примечание:** При сборке файлы автоматически копируются в проект `dashboard` в папку `public/widget/`.

### Шаг 2: Интеграция в ваш проект

#### Вариант 1: UMD (рекомендуется для простых сайтов)

1. Скопируйте файлы из папки `dist/`:
   - `widget.umd.js` - основной файл виджета
   - `index.css` - стили виджета

2. Добавьте контейнер:

```html
<div id="widget"></div>
```

3. Подключите и инициализируйте виджет:

```html
<!-- Подключение стилей виджета -->
<link rel="stylesheet" href="path/to/widget.css" />

<!-- Подключение JavaScript виджета -->
<script src="path/to/widget.umd.js"></script>
```

### Инициализация

````javascript
// Рекомендуемый способ с полной конфигурацией
window.Widget.initWidget({
  appId: "your-app-id", // Обязательный: ID вашего приложения
  containerId: "widget-container", // ID HTML контейнера
  environment: "production", // 'development' или 'production'
  backendUrl: "https://your-backend.com", // URL вашего OIDC сервера
});

// Минимальная конфигурация (остальные параметры используют значения по умолчанию)
window.Widget.initWidget({
  appId: "your-app-id",
  backendUrl: "https://your-backend.com",
});

## 🔗 Интеграция с Dashboard

Виджет автоматически интегрируется с проектом `dashboard` при сборке:

1. Файлы копируются в `packages/dashboard/public/widget/`
2. В `TopBar.tsx` уже размещен элемент `<div id="widget"></div>`
3. В `index.html` dashboard уже подключены файлы виджета

Виджет будет автоматически отображаться в правом верхнем углу TopBar в dashboard приложении.

## 🎨 Особенности

- ✅ **OIDC/OAuth2 аутентификация** - Полная поддержка стандартов безопасности
- ✅ **PKCE Flow** - Защищенный поток авторизации для публичных клиентов
- ✅ **JWKS валидация** - Проверка подписи токенов через JSON Web Key Set
- ✅ **Автоматическое обновление токенов** - Бесшовное продление сессии
- ✅ **Изолированные стили** - CSS виджета не конфликтует с основным сайтом
- ✅ **Автоматическая инициализация** - Работает сразу после подключения
- ✅ **Легкая интеграция** - Один HTML элемент + один JS файл + CSS файл
- ✅ **TypeScript поддержка** - Полная типизация API
- ✅ **Material UI компоненты** - Современный дизайн
- ✅ **Responsive дизайн** - Адаптивная верстка
- ✅ **Конфигурируемость** - Гибкие настройки для разных окружений

## 🛠 Разработка

```bash
# Установка зависимостей
npm install

# Разработка с автоматической пересборкой (рекомендуется)
npm run dev:auto

# Обычная разработка
npm run dev

# Одноразовая сборка
npm run build

# Очистка файлов
npm run clean
````

### Режим автоматической разработки

Команда `npm run dev:auto` запускает nodemon, который отслеживает изменения в папке `src` и автоматически:

1. Пересобирает виджет при изменении файлов `.tsx`, `.ts`, `.css`
2. Копирует обновленные файлы в `packages/dashboard/public/widget/`
3. Позволяет видеть изменения в dashboard без ручной пересборки

Для запуска в VS Code также доступна задача **"dev miniWidget (auto-rebuild)"** в панели задач.

# Запуск в режиме разработки

npm run dev

# Сборка для продакшена

npm run build

# Предварительный просмотр сборки

npm run preview

```

## 📁 Структура проекта

```

miniWidget/
├── src/
│ ├── components/
│ │ ├── App.tsx
│ │ ├── App.module.css
│ │ ├── AccountPopover.tsx
│ │ └── AccountPopover.module.css
│ ├── styles/
│ │ └── index.css
│ ├── types/
│ │ └── index.ts
│ └── main.tsx
├── public/
│ └── index.html
├── dist/ (после сборки)
│ ├── widget.umd.js
│ └── widget.css
├── test-integration.html (пример интеграции)
└── README.md

````

## 🧪 Тестирование интеграции

Откройте файл `test-integration.html` в браузере для проверки работы виджета. Тестовая страница содержит:

- Пример инициализации с API
- Консоль результатов для отладки

## 🔧 Конфигурация сборки

Проект использует Rollup для сборки библиотеки с настройками:

- **Формат:** CommonJS и ES Modules
- **Точка входа:** `src/index.tsx`
- **CSS:** CSS модули с PostCSS
- **Библиотеки:** Material UI как peer dependencies
- **TypeScript:** Полная типизация с генерацией .d.ts файлов

## 📋 Конфигурация

```typescript
interface WidgetConfig {
  appId: string;              // ID вашего приложения
  backendUrl: string;         // URL вашего бэкенда
  redirectUrl?: string;       // URL для редиректа после авторизации
  containerId?: string;       // ID контейнера (необязательно)
  withOutHomePage?: boolean;  // Автоматический редирект на авторизацию
  scopes?: string[];          // OAuth2 scopes
  accentColor?: string;       // Цвет акцента (#hex формат)
}
````

### Пример полной конфигурации

```javascript
const config = {
  appId: "app-123",
  backendUrl: "https://api.example.com",
  redirectUrl: "https://yoursite.com/callback",
  withOutHomePage: false,
  scopes: [
    "openid",
    "email",
    "phone",
    "accounts",
    "lk",
    "profile",
    "offline_access",
  ],
  accentColor: "#ff6b35",
};
```

## 🎯 Примеры интеграции

### React

```jsx
import { useEffect, useRef } from "react";
import { createWidget } from "widget";

function AuthWidget() {
  const widgetRef = useRef(null);

  useEffect(() => {
    const config = {
      appId: "your-app-id",
      backendUrl: "https://your-backend.com",
      accentColor: "#b5262f",
    };

    const widget = createWidget(config);
    widget.mount("widget");

    return () => widget.destroy();
  }, []);

  return <div id="widget" />;
}
```

### Vue 3

```vue
<template>
  <div id="widget"></div>
</template>

<script setup>
import { onMounted, onUnmounted } from "vue";
import { createWidget } from "widget";

let widget = null;

onMounted(() => {
  const config = {
    appId: "your-app-id",
    backendUrl: "https://your-backend.com",
    accentColor: "#b5262f",
  };

  widget = createWidget(config);
  widget.mount("widget");
});

onUnmounted(() => {
  if (widget) {
    widget.destroy();
  }
});
</script>
```

### Angular

```typescript
import { Component, OnInit, OnDestroy } from "@angular/core";
import { createWidget } from "widget";

@Component({
  selector: "app-auth-widget",
  template: '<div id="widget"></div>',
})
export class AuthWidgetComponent implements OnInit, OnDestroy {
  private widget: any;

  ngOnInit() {
    const config = {
      appId: "your-app-id",
      backendUrl: "https://your-backend.com",
      accentColor: "#b5262f",
    };

    this.widget = createWidget(config);
    this.widget.mount("widget");
  }

  ngOnDestroy() {
    if (this.widget) {
      this.widget.destroy();
    }
  }
}
```

## 🛠️ API

### WidgetInstance

```typescript
interface WidgetInstance {
  mount(containerId: string): void; // Монтирование в DOM
  unmount(): void; // Демонтирование (с сохранением экземпляра)
  destroy(): void; // Полное уничтожение экземпляра
}
```

### Методы управления

```javascript
const widget = window.Widget.create(config);

// Монтирование
widget.mount("container-id");

// Временное демонтирование
widget.unmount();

// Повторное монтирование
widget.mount("container-id");

// Полное уничтожение
widget.destroy();
```

## 🎨 Кастомизация

### Цветовая схема

Виджет поддерживает настройку акцентного цвета через параметр `accentColor`:

```javascript
const config = {
  // ... другие параметры
  accentColor: "#ff6b35", // Оранжевый
  // accentColor: '#4CAF50'  // Зеленый
  // accentColor: '#2196F3'  // Синий
};
```

### Shadow DOM изоляция

Виджет использует Shadow DOM для полной изоляции стилей. Это означает:

- ✅ Стили виджета не влияют на ваш сайт
- ✅ Стили вашего сайта не влияют на виджет
- ✅ Предсказуемый внешний вид в любом окружении

## 📊 Сравнение версий

| Характеристика    | React версия         | Vanilla JS версия        |
| ----------------- | -------------------- | ------------------------ |
| Размер бандла     | ~150KB+ gzipped      | ~6KB gzipped             |
| Зависимости       | React, Material UI   | Нет внешних зависимостей |
| Совместимость     | Только React проекты | Любые проекты            |
| Изоляция стилей   | Частичная            | Полная (Shadow DOM)      |
| Скорость загрузки | Медленная            | Быстрая                  |

## 🔧 Разработка

### Структура проекта

```
src/
├── components/
│   ├── App.ts              # Основной компонент
│   ├── AccountPopover.ts   # Попап с информацией о пользователе
│   ├── App.css            # Стили основного компонента
│   └── AccountPopover.css # Стили попапа
├── helpers/
│   └── auth.ts            # Утилиты для аутентификации
├── icons/
│   └── index.ts           # SVG иконки
├── types/
│   └── index.ts           # TypeScript типы
├── utils/
│   └── dom.ts             # Утилиты для работы с DOM
├── global.d.ts            # Глобальные типы
└── index.ts               # Точка входа
```

### Команды

```bash
npm run dev         # Запуск dev сервера
npm run build       # Сборка для продакшена
npm run preview     # Просмотр собранной версии
npm run lint        # Проверка кода
npm run clean       # Очистка dist папки
```

### Тестирование

- `index.html` - основная демо страница
- `test-vanilla.html` - тест UMD версии
- `test-integration.html` - тест интеграции

## 📝 Миграция

Подробное руководство по миграции с React версии на Vanilla JS версию доступно в [MIGRATION.md](./MIGRATION.md).

## 🤝 Поддержка

При возникновении проблем:

1. Проверьте [примеры интеграции](#-примеры-интеграции)
2. Изучите [руководство по миграции](./MIGRATION.md)
3. Создайте issue в репозитории

## 📄 Лицензия

MIT License
